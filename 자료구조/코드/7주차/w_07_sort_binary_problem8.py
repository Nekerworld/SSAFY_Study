# 8. Stable Sort로 좌석표 다시 정리하기

# 공연장 좌석 예약 목록이 다음과 같이 들어왔다.
#
# 각 데이터는 (예약순서, 구역)이다.
#
# 처음 예약된 순서는 그대로 유지하면서
# 구역만 A, B 순서로 정렬하고 싶다.
#
# Python의 정렬은 Stable Sort(안정 정렬)이므로
# 같은 구역끼리는 기존 상대적 순서가 유지된다.
#
# [입력 순서]
# (1, "B"), (2, "A"), (3, "B"), (4, "A"), (5, "B")
#
# [목표]
# [(2, 'A'), (4, 'A'), (1, 'B'), (3, 'B'), (5, 'B')]

reservations = [
    (1, "B"),
    (2, "A"),
    (3, "B"),
    (4, "A"),
    (5, "B"),
]

# 구역만 기준으로 정렬하세요.
# 같은 구역 안에서는 기존 예약순서가 그대로 유지되어야 합니다.
result = None

print(result)
